# Copy this file to .env.local for local development with `vercel dev`.
# In production, set these as Environment Variables in your Vercel
# project settings instead (Project -> Settings -> Environment Variables).

GEMINI_API_KEY=api_key
# Optional, defaults to gemini-3.6-flash if not set:
GEMINI_MODEL=gemini-3.6-flash
